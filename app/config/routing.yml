app:
    resource: "@AppBundle/Controller/"
    type: annotation
    prefix: /

frontend:
    resource: "routing/frontend.yml"

app_backend:
    resource: "routing/backend.yml"
    prefix: /admin

app_api:
    resource: "routing/api.yml"
    prefix: /api
    defaults:
        _format: json

partial:
    resource: "routing/partial.yml"
    prefix: /_partial

_liip_imagine:
    resource: "@LiipImagineBundle/Resources/config/routing.xml"

fos_js_routing:
    resource: "@FOSJsRoutingBundle/Resources/config/routing/routing.xml"

_liip_imagine:
    resource: "@LiipImagineBundle/Resources/config/routing.xml"