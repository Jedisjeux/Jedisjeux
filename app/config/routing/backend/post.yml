app_backend_post:
    resource: |
        alias: app.post
        section: backend
        templates: :backend/crud
        except: ['create', 'show']
        grid: app_backend_post
        redirect: index
        vars:
            all:
                subheader: app.ui.manage_posts
            index:
                icon: comments
    type: sylius.resource