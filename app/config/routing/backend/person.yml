admin_person:
    resource: |
        alias: app.person
        section: backend
        templates: :backend/crud
        redirect: index
        grid: app_backend_person
        vars:
            all:
                subheader: app.ui.manage_people
                templates:
                    form: backend/person/_form.html.twig
            index:
                icon: spy
    type: sylius.resource