admin_person:
    resource: |
        alias: app.person
        section: backend
        templates: :backend/crud
        redirect: index
        grid: app_backend_person
        vars:
            all:
                subheader: sylius.ui.manage_people
            index:
                icon: spy
    type: sylius.resource