app_backend_partial_session_count_per_day:
    methods: [GET]
    path: /count-per-day/
    defaults:
        _controller: AppBundle:Session:sessionCountPerDay

app_backend_partial_session_count_per_month:
    methods: [GET]
    path: /count-per-month/
    defaults:
        _controller: AppBundle:Session:sessionCountPerMonth