app_api_notification:
    resource: "api/notification.yml"
    options:
      expose: true

app_api_product:
    resource: "api/product.yml"
    options:
      expose: true

app_api_page_views:
    resource: "api/page_views.yml"
    options:
      expose: true

app_api_product_review:
    resource: "api/product_review.yml"
    options:
      expose: true

app_api_search:
    resource: "api/search.yml"
    options:
      expose: true

app_api_article:
    resource: "api/article.yml"
    options:
      expose: true

app_api_person:
    resource: "api/person.yml"
    options:
      expose: true