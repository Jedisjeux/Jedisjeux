app_partial_article_index:
    methods: [GET]
    path: /articles/
    defaults:
        _controller: app.controller.article:indexAction
        _sylius:
            criteria: $criteria
            template: $template
            paginate: $paginate
            sortable: true
            sorting: $sorting
    requirements:
        template: "[^?]+"