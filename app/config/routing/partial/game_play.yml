sylius_partial_game_play_index:
    methods: [GET]
    path: /game-plays/
    defaults:
        _controller: app.controller.game_play:indexAction
        _sylius:
            criteria: $criteria
            template: $template
            paginate: $paginate
            sortable: true
            sorting: $sorting
    requirements:
        template: "[^?]+"