sylius_grid:
    grids:
        app_backend_staff_list:
            driver:
                name: doctrine/orm
                options:
                    class: "%app.model.staff_list.class%"
            sorting:
                name: asc
            fields:
                name:
                    type: string
                    label: sylius.ui.name
                    sortable: name
            filters:
                search:
                    type: string
                    label: sylius.ui.search
                    options:
                        fields: [name]
            actions:
                main:
                    create:
                        type: create
                item:
                    update:
                        type: update
                    delete:
                        type: delete
