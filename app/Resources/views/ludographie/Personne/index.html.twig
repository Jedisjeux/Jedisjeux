{% extends "base.html.twig" %}
{% from 'JDJWebBundle:Macros:misc.html.twig' import pagination %}

{% block body -%}

    <h2>Les ludographies</h2>

    {% include 'ludographie/personne/partial/_list.html.twig' with {'personnes': entities} %}
    <div class="pagination-wrapper">
        <div class="pagination-bottom">
            {{ pagination(entities) }}
        </div>
    </div>
{% endblock %}