{% macro blogCommentItem(post) %}
    {% import 'macros/items.html.twig' as item %}
    {% from 'macros/users.html.twig' import avatar %}
    {% import 'macros/buttons.html.twig' as button %}

    {% set customer = post.createdBy %}
    {% set imagePath = avatar(customer) %}

    {% set meta %}
        <!-- Date -->
        <a href="#"><i class="fa fa-calendar"></i> &nbsp; {{ post.createdAt|date('d/m/Y') }}</a>
        &nbsp;
        <!-- Author -->
        <a href="#"><i class="fa fa-user"></i> &nbsp; {{ customer }}</a>
        &nbsp;
        <a href="#">
            <i class="fa fa-thumbs-up"></i>
            <span data-post-id="{{ post.id }}" class="nbLikes">0</span>
        </a>
        &nbsp;
        <a href="#">
            <i class="fa fa-thumbs-down"></i>
            <span data-post-id="{{ post.id }}" class="nbDislikes">0</span>
        </a>
    {% endset %}

    {{ item.blogCommentItem(imagePath, customer.user.username, 'thumbnail', 'img-responsive img-round', null, null, meta, post.body) }}

{% endmacro %}