{% macro avatar(user) %}
    {% set imageSrc = '//ssl.gstatic.com/accounts/ui/avatar_2x.png' %}

    {% set imageSrc = user.avatar ? user.avatar.webPath | imagine_filter('thumbnail') : '//ssl.gstatic.com/accounts/ui/avatar_2x.png' %}

    {{ imageSrc }}
{% endmacro %}