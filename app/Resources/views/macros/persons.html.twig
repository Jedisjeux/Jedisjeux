{% macro image(person, filter) %}
    {% set filter = filter|default('thumbnail') %}

    {% set imageSrc = '//ssl.gstatic.com/accounts/ui/avatar_2x.png' %}

    {% if (person.mainImage) %}
        {% set imageSrc = person.mainImage.webPath | imagine_filter(filter) %}
    {% endif %}

    {{ imageSrc }}
{% endmacro %}