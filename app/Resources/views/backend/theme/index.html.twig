{% extends "layout/backend_index.html.twig" %}
{% from '::macros/misc.html.twig' import pagination %}
{% import 'macros/buttons.html.twig' as button %}
{% from 'macros/sorting.html.twig' import sorting %}
{% import 'macros/modal.html.twig' as modal %}

{% set sorting = app.request.get('sorting') %}

{% set icon = false %}
{% set currentOrder = null %}

{% if null == sorting %}
    {% set icon = true %}
    {% set currentOrder = 'asc' %}
{% endif %}

{% block new_entity_button %}
    <a class="btn btn-primary" href="{{ path('admin_theme_new') }}">
        <span class="glyphicon glyphicon-plus-sign"></span>
        Nouveau thème
    </a>
{% endblock new_entity_button %}

{% block page_title %}
    Liste des thèmes
{% endblock page_title %}

{% block list %}
    <table class="table table-striped">
        <thead>
        <tr>
            <th>#</th>
            <th>{{ sorting('admin_theme_index', 'label.name'|trans, 'name', 'asc', icon, currentOrder) }}</th>
            <th>{{ sorting('admin_theme_index', 'label.description'|trans, 'description', 'asc') }}</th>
        </tr>
        </thead>
        <tbody>
        {% for theme in themes %}
            <tr>
                <td>{{ theme.id }}</td>
                <td>{{ theme }}</td>
                <td>{{ theme.description|striptags }}</td>
                <td class="text-right">
                    <ul class="list-inline text-nowrap">
                        <li>
                            {{ button.show(path('theme_show', { 'id': theme.id, 'slug': theme.slug })) }}
                        </li>
                        <li>
                            {{ button.edit(path('admin_theme_edit', { 'id': theme.id })) }}
                        </li>
                        <li>
                            {{ button.delete(path('admin_theme_delete', { 'id': theme.id })) }}
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock list %}

{% block Pagination %}
    <div class="pagination-wrapper">
        <div class="pagination-bottom">
            {{ pagination(themes) }}
        </div>
    </div>
{% endblock %}