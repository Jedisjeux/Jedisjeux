{% from 'macros/users.html.twig' import avatar %}

{% for game_review in game_reviews %}
    {% set user = game_review.jeuNote.author %}
    <li>
        <a href="{{ path('game_review_show', {'id': game_review.id, 'slug': game_review.jeuNote.jeu.slug}) }}">
            <!-- Item image -->
            <img src="{{ avatar(user) | imagine_filter('thumbnail') }}" alt="" class="img-responsive img-round"/>

            <!-- Item title -->
            <span>{{ game_review.libelle }}</span><br/>

            <div class="rating-container rating-gly-star" data-content="">
                <div class="rating-stars" data-content=""
                     style="width: {{ game_review.jeuNote.note.valeur }}0%;"></div>
            </div>

            <div class="clearfix"></div>
        </a>
    </li>
{% endfor %}