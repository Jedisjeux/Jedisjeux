app_backend_game_award:
    resource: |
        alias: app.game_award
        section: backend
        except: ['show', 'create']
        redirect: update
        grid: app_backend_game_award
        vars:
            all:
                subheader: app.ui.manage_game_awards
            index:
                icon: trophy
        templates: backend/crud
    type: sylius.resource
