# Learn more about services, parameters and containers at
# http://symfony.com/doc/current/book/service_container.html
parameters:
#    parameter_name: value

services:
#    service_name:
#        class: App\Directory\ClassName
#        arguments: ["@another_service_name", "plain_value", "%parameter_name%"]

    app.calculator.topic_count_by_taxon:
        class: App\Calculator\TopicCountByTaxonCalculator
        arguments:
            - "@app.repository.topic"

    app.calculator.product_count_by_taxon:
        class: App\Calculator\ProductCountByTaxonCalculator
        arguments:
            - "@sylius.repository.product"

    app.calculator.product_count_by_person:
        class: App\Calculator\ProductCountByPersonCalculator