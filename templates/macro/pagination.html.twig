{% macro simple(paginator) %}
    {% if paginator.haveToPaginate() %}
        <div class="pagination justify-content-center">
            {{ pagerfanta(paginator, 'frontend_translated') }}
        </div>
    {% endif %}
{% endmacro %}
