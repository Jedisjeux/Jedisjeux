{% import 'SyliusUiBundle:Macro:buttons.html.twig' as buttons %}

{% set routeParameters = grid.requestConfiguration.vars.route.parameters|default([]) %}

{{ buttons.create(path(grid.requestConfiguration.getRouteName('create'), routeParameters)) }}
