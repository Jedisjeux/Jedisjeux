{% from 'macro/image.html.twig' import images %}
{% if (data.productVariant.mainImage) %}
    {{ image.render(data.productVariant.mainImage.webPath, 'thumbnail', data.productVariant, 'ui circular image') }}
{% endif %}