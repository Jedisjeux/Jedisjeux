{% from 'backend/macro/translationForm.html.twig' import translationForm %}

<div class="ui active tab" data-tab="details">
    <h3 class="ui dividing header">{{ 'sylius.ui.details'|trans }}</h3>
    {{ translationForm(form.translations) }}

    <h4 class="ui dividing header">{{ 'label.release_date'|trans }}</h4>
    <div class="two fields">
        {{ form_row(form.releasedAtPrecision) }}
        {{ form_row(form.releasedAt) }}
    </div>
</div>
