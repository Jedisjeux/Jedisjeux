{% from 'backend/macro/translationForm.html.twig' import translationFormWithSlug %}

{% set locale =  app.request.locale %}

<div class="ui active tab" data-tab="details">
    <h3 class="ui dividing header">{{ 'sylius.ui.details'|trans }}</h3>
    {{ translationFormWithSlug(form.translations, 'backend/product/_slugField.html.twig', product) }}
    <div class="ui segment">
        {{ form_row(form.boxContent) }}
    </div>
</div>

<div style="display: none">
    {{ form_row(form.firstVariant.translations[locale].name) }}
</div>