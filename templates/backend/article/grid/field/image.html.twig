{% from 'macro/image.html.twig' import images %}
{% if (data.mainImage and data.mainImage.webPath) %}
    {{ image.render(data.mainImage.webPath, 'thumbnail', data.name, 'ui circular image') }}
{% endif %}