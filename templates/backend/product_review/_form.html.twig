<div class="ui stackable grid">
    <div class="twelve wide column">
        <div class="ui segment">
            {{ form_errors(form) }}
            {{ form_row(form.title) }}
            {{ form_row(form.comment) }}
            {{ form_row(form.rating) }}
        </div>
    </div>
    <div class="four wide column">
        {% include 'backend/product_review/_product.html.twig' %}
        {% include 'backend/product_review/_author.html.twig' %}
    </div>
</div>
