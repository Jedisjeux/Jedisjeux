 <div class="rating md rate"
      data-product-id="{{ app.request.get('productId') }}"
      data-max-value="10" {% if product_review %}data-rate-value="{{ product_review.rating }}"{% endif %}
      data-update-input-field-name="sylius_product_review_rating_rating"
      data-step-size="1">
 </div>
