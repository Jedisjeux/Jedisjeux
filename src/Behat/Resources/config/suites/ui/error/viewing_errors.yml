default:
    suites:
        ui_viewing_errors:
            contexts:
                - app.behat.context.hook.doctrine_orm

                - app.behat.context.ui.frontend.error
            filters:
                tags: "@viewing_errors && @ui"
