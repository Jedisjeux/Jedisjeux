search_autocomplete:
    pattern:  /autocomplete
    defaults: { _controller: "JDJSearchBundle:Search:autoComplete" }
    options:
      expose: true

search_query:
    pattern:  /
    defaults: { _controller: "JDJSearchBundle:Search:searchQuery" }

