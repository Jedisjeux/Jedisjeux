{% extends "JDJWebBundle::layout.html.twig" %}

{% block body -%}
    <h1>{{ entity.libelle }}</h1>

    {{ entity.description|raw }}

    {% include "JDJJeuBundle:Jeu:partial/list.html.twig" with {'jeux': jeux} %}

        <ul class="record_actions">
    <li>
        <a href="{{ path('mechanism') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('mechanism_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
