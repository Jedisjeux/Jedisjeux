{% extends "JDJWebBundle::layout.html.twig" %}

{% block body -%}
    <h1>Liste des modes de règlement</h1>

    <ul>
        <li>
            <a class="button" href="{{ path('cptamodereglement_new') }}">
                Créer un mode de règlement
            </a>
        </li>
    </ul>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Libelle</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('cptamodereglement_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.libelle }}</td>
                <td class="small-3">
                <ul class="inline-list">
                    <li>
                        <a class="small button" href="{{ path('cptamodereglement_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a class="small button" href="{{ path('cptamodereglement_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    {% endblock %}
